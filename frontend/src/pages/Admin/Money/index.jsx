import styles from './Money.module.scss';
import classNames from 'classnames/bind';
const cx = classNames.bind(styles);

function AdminMoney() {
    return <h2>Thống kê</h2>;
}

export default AdminMoney;